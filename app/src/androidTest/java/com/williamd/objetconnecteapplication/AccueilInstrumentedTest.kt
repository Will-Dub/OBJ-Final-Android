package com.williamd.objetconnecteapplication
/*
import androidx.test.espresso.Espresso.onView
import androidx.test.espresso.action.ViewActions.click
import androidx.test.espresso.assertion.ViewAssertions.matches
import androidx.test.espresso.matcher.ViewMatchers.withId
import androidx.test.espresso.matcher.ViewMatchers.withText
import androidx.test.ext.junit.rules.ActivityScenarioRule
import androidx.test.platform.app.InstrumentationRegistry
import androidx.test.ext.junit.runners.AndroidJUnit4

import org.junit.Test
import org.junit.runner.RunWith

import org.junit.Assert.*
import org.junit.Rule


@RunWith(AndroidJUnit4::class)
class AccueilInstrumentedTest {
    @Rule
    @JvmField
    val activityRule = ActivityScenarioRule(MainActivity::class.java)

    @Test
    fun testTextViewUpdatedAfterDataRetrieval() {
        // Simule un click sur un bouton pour démarrer la récupération des données
        onView(withId(R.id.someBtn)).perform(click())

        // Vérifie que le textView est mis à jour avec les nouvelles données
        onView(withId(R.id.someTv)).check(matches(withText("...")))
    }
}*/